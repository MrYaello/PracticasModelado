Es importante que primero se ejecute la clase Servidor y después la clase Cliente para que el código funcione correctamente:D. 
Cada vez que se vuelva a compilar, tambien es necesario cambiar el numero de puerto, ya que si no, aparecerá una leyenda de que el puerto 
ya está siendo utilizado.
Gracias.